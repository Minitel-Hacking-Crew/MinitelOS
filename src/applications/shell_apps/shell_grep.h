#ifndef SHELL_GREP_H
#define SHELL_GREP_H

#include "globals.h"

void shell_grep(const String &args);
void shell_head(const String &args);
void shell_tail(const String &args);

#endif // SHELL_GREP_H
